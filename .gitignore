node_modules/

/.cache
/build
/.react-router

.env
.DS_Store
npm-debug.log*

# Local-only pnpm settings file (this sandbox's supply-chain wrapper writes it).
# It is NOT a real workspace definition and breaks pnpm 9 on Railway
# ("packages field missing or empty"), so it must never be deployed.
pnpm-workspace.yaml
